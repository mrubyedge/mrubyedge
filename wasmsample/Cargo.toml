[package]
name = "wasmsample"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies.mrubyedge]
version = "1.1.9"

# [profile.release]
# # Tell `rustc` to optimize for small code size.
# opt-level = "s"
